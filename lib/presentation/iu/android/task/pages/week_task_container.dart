import 'package:flutter/material.dart';

class WeekTaskContainer extends StatelessWidget {
  const WeekTaskContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
