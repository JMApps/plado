import 'package:flutter/material.dart';

class MonthTaskContainer extends StatelessWidget {
  const MonthTaskContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
